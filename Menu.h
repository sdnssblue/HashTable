#pragma once

/// @brief Функция для ввода строки.
/// @param text - Строка для вывода информации.
/// @return Возвращает строку.
std::string InputString(const char* text);

void Show(Element* element);

/// @brief Основное меню.
int main();